#include "header.h"
/*
int	main(void)
{
	t_ray	ray;
	t_obj	obj;
	t_mtrx	mt[MAX_TRANSF];
	t_ray	local;

	ray.hit = NULL;
	ray.closest = NULL;
	obj.is_transformed = FALSE;
	create_tupple(&ray.og, 1, 2, 3);
	create_vector(&ray.dir, 0, 1, 0);
	copy_ray(&local, &ray);
	printf("origin local ray: \n");
	print_t_vec(local.og);
	printf("\ndir local ray\n");
	print_t_vec(local.dir);
	printf("\n\nmatrix\n");
	scalar(&mt[0], 2, 3, 4);
	print_matrix(mt[0], 4);
	transform_object(mt, 1, &obj);
	transform_ray(ray, &local, obj);
	printf("\n\norigin local ray: \n");
	print_t_vec(local.og);
	printf("\ndir local ray\n");
	print_t_vec(local.dir);
}*/
